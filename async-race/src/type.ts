export type CarParam = {
    name: string;
    color: string;
};

// export type CarPatch = {
//     velocity: string;
//     distance: string;
// };

export type Car = {
    name: string;
    color: string;
};

export type dataType = {
    name: string;
    color: string;
    id: number;
};
