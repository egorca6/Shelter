export type CarParam = {
    name: string;
    color: string;
};

export type dataType = {
    name: string;
    color: string;
    id: number;
};

export type winsOnPage = {
    id: number;
    wins: number;
    time: number;
};
